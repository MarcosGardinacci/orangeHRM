/**
 * Mensagens de validação e alertas retornados pela aplicação OrangeHRM
 */
export const ERROR_MESSAGES = {
  INVALID_CREDENTIALS: 'Invalid credentials',
  REQUIRED_FIELD: 'Required',
} as const;
